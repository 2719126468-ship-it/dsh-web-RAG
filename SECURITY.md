# 安全政策

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 最新版（master） | ✅ 支持 |

## 报告漏洞

如果你发现了安全漏洞，**请不要直接开 issue**，通过以下方式私下报告：

1. 在 GitHub 仓库的 Security 标签页使用 "Report a vulnerability"
2. 或通过邮件联系仓库维护者

请在报告中包含：

- 漏洞类型
- 复现步骤
- 潜在影响
- 可能的修复建议（如有）

我们会在收到报告后 72 小时内回复，确认问题后尽快发布修复。

## 注意事项

本项目需要 `DEEPSEEK_API_KEY` 才能调用大语言模型。

- **不要**把 `.env` 文件提交到 Git
- **不要**在 issue / PR / 截图里暴露 API Key
- 如果 Key 意外泄露，请立即到 DeepSeek 平台吊销并重新生成

## 已知限制

- 本项目默认在本地运行，向量数据库 `qdrant_data/` 存储在本地磁盘
- 上传的文档不会被发送到除 DeepSeek API 之外的服务
