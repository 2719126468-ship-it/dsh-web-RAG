# LLM
langchain>=1.0
langchain-community>=0.4.2
langchain-core>=1.0
langchain-openai>=0.2
langchain-huggingface>=0.1
langchain-text-splitters>=0.1
langchain-qdrant>=0.1

# Vector store
qdrant-client>=1.15,<1.20

# Embeddings & Reranking
sentence-transformers>=2.7
huggingface-hub>=0.20

# Document loaders
unstructured>=0.13
markdown>=3.6
pypdf>=4.0
python-docx>=1.1
docx2txt>=0.8
pdfplumber>=0.10   # smarter PDF parsing (handles two-column layouts)

# Hybrid retrieval
rank_bm25>=0.2

# File monitoring (optional, for src/watcher.py)
watchdog>=3.0

# Notion sync (optional, for src/notion_sync.py)
# notion-client>=2.0

# UI
streamlit>=1.36

# Utilities
python-dotenv>=1.0
tiktoken>=0.7
numpy>=2.0

# Dev (optional, for generating test PDFs)
reportlab>=4.0

# PPTX 支持
python-pptx>=0.6.23

# Excel 支持
openpyxl>=3.1.0

# 可选：DeepDoc PDF 解析器（RAGFlow 抽取版），需要时手动安装
# deepdoc-pdfparser>=0.1.0

# RAGAS 评估（可选，较重）
# ragas>=0.1.9
# datasets>=2.18.0

# 多模态（可选）
# PyMuPDF>=1.24.0

# API 服务（可选）
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0
